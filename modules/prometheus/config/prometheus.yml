global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "backend"
    static_configs:
      - targets: ['backend_container:3000']
    metrics_path: /app/metrics

  - job_name: "nginx"
    static_configs:
      - targets: ['nginx_container:80']
