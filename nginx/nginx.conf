server {
    listen 80;

    location / {
        root /usr/share/nginx/html; # Angular build fájlok helye
        index index.html;

        try_files $uri /index.html; # SPA routing
    }

    location /assets/ {
        root /usr/share/nginx/html;
    }

    error_page 404 /index.html;
}
